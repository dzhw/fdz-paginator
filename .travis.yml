language: node_js

node_js:
  - lts/*

before_script:
  - npm run build

script:
  - npm run test
after_success:
  - npm run build
  - npm run docs

deploy:
  - provider: npm
    email: $NPM_EMAIL
    api_key: $NPM_AUTH_TOKEN
    skip_cleanup: true
    on:
      tags: true
      branch: master
