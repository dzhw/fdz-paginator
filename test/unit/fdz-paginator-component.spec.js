describe('fdz-paginator component ', function() {
  // Error in angular.mock.module -> Unknown provider

  // let elm, scope;
  //
  // beforeEach(angular.mock.module('fdzPaginatorModule'));
  // beforeEach(angular.mock.module('templates'));
  // beforeEach(inject(function($rootScope, $compile) {
  //   elm = angular.element('<fdz-paginator template-url="test.html"></fdz-paginator>');
  //   scope = $rootScope;
  //   $compile(elm)(scope);
  //   scope.$digest();
  // }));
  // it('should load the template from the attribute template-url', function() {
  //  let h1 = elm.find('h1');
  //  expect(h1.eq(0).text()).toBe('Test');
  // });
});
